<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link rel="stylesheet" href="/styles/main.css" />
    <link rel="stylesheet" href="/styles/layout.css" />
    <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
    <link rel="stylesheet" href="/vendor/border-box.css" />
    <link rel="stylesheet" href="/styles/nav.css" />
    <link rel="stylesheet" href="/styles/edit-profile.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <%- include('partials/_nav.ejs') %>
    <div class="container">
      <a href="/users/<%= userId %>" class="back-button">
        <i class="fa-solid fa-xmark"></i>
      </a>
      <div class="container-content">
        <div class="edit-profile-title">
          <h1>Edit Profile</h1>
          <p>Information you add here is visible to anyone who can view your profile.</p>
        </div>
        <form action="/api/users/<%= userId %>/edit" method="POST">
            <div class="form-inputs">
              <label for="firstName">Name</label>
              <input type="text" id="firstName" name="firstName" value='<%= profile.name %>' required>
            </div>
            <div class="form-inputs">
              <label for="pronouns">Pronouns</label>
              <h6 id="why-pronouns"><a href="https://lgbt.foundation/pronouns#:~:text=It%E2%80%99s%20good%20practice,binary%20community%20members." target="_blank">Why pronouns?</a></h6>
              <input type="text" id="pronouns" name="pronouns" value="<%= profile.pronouns %>">
            </div>
            <div class="form-inputs">
              <label for="username">Username</label>
              <input type="text" id="username" name="username" value="<%= profile.username %>" required>
            </div>
            <div class="form-inputs">
                <label for="about">About</label>
                <textarea id="about" name="about" rows="4" cols="50" required><%= profile.about_me %></textarea>
            </div>
            <button type="submit" id="save-button">Save</button>
        </form>
      </div>

    </div>
</body>
</html>
